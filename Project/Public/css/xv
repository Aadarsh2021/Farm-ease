/* Banner Container */
.banner_container {
    margin: 40px 0;
    padding: 0 20px; /* Add padding for spacing */
}

/* Category Heading */
.category_heading {
    text-transform: uppercase;
    color: #3e4152;
    letter-spacing: .15em;
    font-size: 1.8em;
    margin: 50px 0 10px 0; /* Adjust margin */
    max-height: 5em;
    font-weight: 700;
}

/* Items Container */
.category-items, .items-container {
    width: 100%; /* Adjust width for full width */
    margin: 50px 0; /* Adjust margin for spacing */
    display: flex;
    flex-wrap: wrap;
    justify-content: space-evenly;
}

/* Footer Container */
.footer_container {
    padding: 30px 20px 40px 20px; /* Add padding for spacing */
    background: #fafcfa;
    display: flex;
    justify-content: space-between; /* Adjust alignment */
    flex-wrap: wrap; /* Allow items to wrap */
}